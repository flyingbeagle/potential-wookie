# potential-wookie

This is a simple node command line program to grab the stories that are embedded in the description tag (rss:description). every week, four new stories are added to the RSS feed.

